Use basic Amazon Linux EC2 64bit

# Connecting to the instance
You'll need to connect like so: ssh -i etc/pdhex.pem ec2_dns_here

# Install Git
sudo yum install git-core

# Install rubygems (it comes with ruby 1.8.7)
sudo yum install rubygems

# Install rails
sudo gem install rails -v=2.3.5 --no-rdoc --no-ri

# Install heroku gem
sudo gem install heroku

# Create a keypair
ssh-keygen (accept all defaults) 

# Add the keys you just made
heroku keys:add

# Get the code
git clone  git@heroku.com:pdhex.git

# Installing sqlite
  # installing gcc
  sudo yum install gcc

  # Upgrading ruby
    # Downloading ruby
    wget ftp://ftp.ruby-lang.org//pub/ruby/1.9/ruby-1.9.2-p0.tar.gz
    tar -xvf ruby-1.9.2-p0.tar.gz
    cd ruby-1.9.2-p0.tar.gz
    
    # Build
    ./configure
    make
    make install
